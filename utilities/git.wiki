= Contents =
  - [[#git|git]]
      - [[#git#concept|concept]]
      - [[#git#configuration|configuration]]
        - [[#git#configuration#gitignore|gitignore]]
      - [[#git#tips and tricks|tips and tricks]]
        - [[#git#tips and tricks#checkout one file|checkout one file]]
        - [[#git#tips and tricks#checkout remote branch|checkout remote branch]]
        - [[#git#tips and tricks#check why file ignored|check why file ignored]]
        - [[#git#tips and tricks#delete local branch|delete local branch]]
        - [[#git#tips and tricks#delete remote branch|delete remote branch]]
        - [[#git#tips and tricks#diff with branch|diff with branch]]
        - [[#git#tips and tricks#exclude w/o .gitignore|exclude w/o .gitignore]]
        - [[#git#tips and tricks#index: show files in index|index: show files in index]]
        - [[#git#tips and tricks#push to remote|push to remote]]
        - [[#git#tips and tricks#push to remote (diff name)|push to remote (diff name)]]
        - [[#git#tips and tricks#remove file from index|remove file from index]]
        - [[#git#tips and tricks#remove all files from index|remove all files from index]]
        - [[#git#tips and tricks#skipping files|skipping files]]
        - [[#git#tips and tricks#stash: apply 1 file|stash: apply 1 file]]
        - [[#git#tips and tricks#stash: clear|stash: clear]]
        - [[#git#tips and tricks#stash: show|stash: show]]
        - [[#git#tips and tricks#submodules: update|submodules: update]]
        - [[#git#tips and tricks#urls: change remote url|urls: change remote url]]

= git =
=== concept ===
*workspace*:: is the directory tree of (source) files that you see and edit
*index*:: is a single, large, binary file in `.git/index`, which lists all files in the current branch, their sha1 checksums, time stamps and the file name
*local repository*:: is a hidden directory (`.git`) including an objects directory containing all versions of every file in the repo (local branches and copies of remote branches) as a compressed "blob" file

{{{

      +------commit -a---------->>+
      |                           |
      +------add-->>+---commit-->>+
      |             |             +-----push---->>+
      |             |             |               |
 +----+----+      +-+---+     +---+------+    +---+-------+
 |workspace|      |index|     |local repo|    |remote repo| 
 +----+----+      +-+---+     +-------+--+    +---+-------+
      |             |                 |           |
      +<<------------pull or rebase---------------+
      |             |                 |           |    
      |             |                 +<<--fetch--+
      |             |                 |
      +<<-----checkout HEAD-----------+
      |             |                 |
      +<<-checkout--+                 |
      |             |                 |
      +<------diff HEAD-------------->+
      |             |
      +<---diff---->+   

}}}                                   
=== configuration ===                 

==== gitignore ====
{{{
  # exclude everything
  *
  # except
  !*.extension
  # even if they are in subfolders
  !*/
}}}

=== tips and tricks ===

==== checkout one file ====
{{{
  git checkout $commit_hash -- $filename
}}}

==== checkout remote branch ====
{{{
  git checkout $remote_branch
}}}

==== check why file ignored ====
{{{
  git check-ignore -v $filename
}}}

==== delete local branch ====
{{{
  git branch -d $branch
}}}

==== delete remote branch ====
{{{
  git push origin --delete $remote_branch
}}}

==== diff with branch ====
{{{
  git diff --name-only $branch
}}}

==== exclude w/o .gitignore ====
{{{
   .git/info/exclude
}}}

==== index: show files in index ====
{{{
  git ls-tree -r master --name-only
}}}

==== push to remote ====
{{{
  git push -u $remote $branch
}}}

==== push to remote (diff name) ====
{{{
  git push -u $remote HEAD:$remote_branch
}}}

==== remove file from index ====
{{{
  git reset $file
}}}

==== remove all files from index ====
{{{
  git reset
}}}

==== skipping files ====
`git update-index`
1. `--assume-unchanged`
Git assumes that files with this bit are not changed by user and skips their check 
2. `--skip-worktree`
Git assumes that working copy of files with this bit is up to date with index and use version from the index. Meanwhile the file in the working tree can be different or even absent. 
<br>
in both cases Git:
- won't commit this file (even if it is added explicitly)
- _can_ update working copy of the file if it's changed on remote side
- will show merge conflict if file is changed on both sides

==== stash: apply 1 file ====
{{{
  git checkout stash@{0} -- $file
}}}

==== stash: clear ====
{{{
  git stash clear
}}}

==== stash: show ====
{{{
  git stash show -p
}}}

==== submodules: update ====
{{{
  git submodule update --recursive --remote
  git submodule update --init --remote --rebase --recursive
}}}
- `--init` initialize all submodules, which haven't been yet
- `--checkout` checkout on commit, recorded in superproject (detached head)
- `--rebase` rebase on commit, recorded in superproject (no detached head)
- `--recursive` update nested submodules
- `--remote` use the status of the submodule's remote-tracking branch (branch name is taken from .gitmodules)

==== urls: change remote url ====
{{{
  git remote set-url $remote $new_url
}}}
